version: 1

update_configs:
  - package_manager: "javascript"
    directory: "/"
    update_schedule: "live"

    allowed_updates:
      - match:
          dependency_name: "@ecomplus/storefront-*"

    commit_message:
      prefix: "chore"
      include_scope: true
